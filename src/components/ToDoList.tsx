export {}

/*
function ToDoList({ toDoList, onItemClick }) {
  return (
    <>
      {toDoList.map((toDo) => (
        <IonCard key={toDo.id} onClick={() => onItemClick(toDo)}>
          <IonCardHeader>
            <IonCardTitle>{toDo.title}</IonCardTitle>
          </IonCardHeader>
          <IonCardContent>{toDo.description}</IonCardContent>
        </IonCard>
      ))}
    </>
  );
}
*/